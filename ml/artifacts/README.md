# Place trained model.pt, scaler.json, metrics.json here after `python ml/train.py`.
# model.pt is a dict: {"kind": "hybrid", "state": <state_dict>}.
# yolov8n.pt is the COCO object detector used for "what's in the frame" (downloaded on first run).
